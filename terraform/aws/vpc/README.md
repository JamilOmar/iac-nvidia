### VPC

It creates the VPC

- 1 VPC


### How To Start

- Update the backend information in the terrafor main file.
The run the terraform commands:

```sh
terraform init
terraform plan
terraform apply
```
